using System;
using System.Linq;

namespace ConvertingEnum
{
    static class EnumConverter
    {
        public static T GetIntegerValue<T>(Enum value) where T: struct
        {
            #region Please implement the method
            
            /*
             * In this test, we will create a function that converts any enum value to integer type.
             * Please note that the enum values can be any integral type, e.g. int, sbyte and short.
             * The method should deal with many conditions like transfer to compatible integeral
             * type (e.g. an uint enum value can be converted to ulong), as well as value overflow
             * check.
             *
             * Difficulty: Medium
             */
            
            throw new NotImplementedException();
            
            #endregion
        }
    }
}